import { HoraCortaPipe } from './hora-corta.pipe';

describe('HoraCortaPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraCortaPipe();
    expect(pipe).toBeTruthy();
  });
});
